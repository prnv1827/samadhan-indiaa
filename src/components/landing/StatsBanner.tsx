import React from 'react';import {useChallenges} from '../../contexts/ChallengeContext';
export function StatsBanner(){const{all}=useChallenges();const approved=all.filter(c=>c.status!=='pending'&&c.status!=='rejected').length;const states=new Set(all.map(c=>c.state).filter(Boolean)).size;const universities=new Set(all.map(c=>c.teamApplication?.university).filter(Boolean)).size;const solved=all.filter(c=>c.status==='resolved').length;return <section aria-labelledby="stats-heading" className="border-b border-forest-900 bg-forest-800"><div className="mx-auto grid w-full max-w-[1200px] gap-10 px-6 py-14 lg:grid-cols-[0.9fr_1.1fr] lg:items-center lg:py-16"><div><h2 id="stats-heading" className="text-xs font-semibold uppercase tracking-[0.16em] text-forest-200">Live platform data</h2><p className="mt-5 font-serif text-[4rem] font-semibold leading-none text-white sm:text-[5rem]">{all.length}</p><p className="mt-3 text-lg font-medium text-forest-100">Total reports</p><p className="mt-1 text-sm text-forest-200">from real submissions</p></div><dl className="grid gap-x-8 gap-y-8 border-t border-forest-700 pt-8 sm:grid-cols-3 lg:border-l lg:border-t-0 lg:pl-12 lg:pt-0"><div><dd className="font-serif text-3xl font-semibold text-white">{approved}</dd><dt className="mt-2 text-sm font-medium text-forest-100">Approved / active</dt><p className="mt-1 text-xs text-forest-200">visible after Admin review</p></div><div><dd className="font-serif text-3xl font-semibold text-white">{states}</dd><dt className="mt-2 text-sm font-medium text-forest-100">States / UTs represented</dt><p className="mt-1 text-xs text-forest-200">live location data</p></div><div><dd className="font-serif text-3xl font-semibold text-white">{universities}</dd><dt className="mt-2 text-sm font-medium text-forest-100">University teams</dt><p className="mt-1 text-xs text-forest-200">real applications</p></div></dl></div></section>}
