import React,{useEffect,useState} from 'react';
import {GraduationCapIcon,TrophyIcon} from 'lucide-react';
import {useChallenges} from '../../contexts/ChallengeContext';
export function UniversityLeaderboard(){const{universityLeaderboard,all}=useChallenges();const[rows,setRows]=useState<any[]>([]);useEffect(()=>{let live=true;const load=async()=>{try{const r=await universityLeaderboard();if(live)setRows(r)}catch{} };void load();return()=>{live=false}},[universityLeaderboard,all.length]);return <section className="border-b border-line bg-surface"><div className="mx-auto w-full max-w-[1200px] px-6 py-16 lg:py-20"><div className="flex items-end justify-between gap-6"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-forest-600">University leaderboard</p><h2 className="mt-2 font-serif text-3xl font-semibold tracking-tight text-ink sm:text-4xl">Who is solving the most problems?</h2><p className="mt-3 max-w-2xl text-base leading-relaxed text-ink-soft">Ranked by verified solutions and resolved community problems, with active solution work also counted.</p></div><TrophyIcon className="hidden h-10 w-10 text-clay-500 sm:block"/></div>{rows.length?<div className="mt-8 overflow-hidden rounded-card border border-line"><div className="grid grid-cols-[44px_1fr_repeat(3,100px)] gap-3 bg-canvas px-4 py-3 text-xs font-semibold uppercase tracking-wide text-ink-muted"><span>#</span><span>University / college</span><span>Resolved</span><span>Solutions</span><span>Worked on</span></div>{rows.map((r,i)=><div key={r.university} className="grid grid-cols-[44px_1fr_repeat(3,100px)] gap-3 border-t border-line px-4 py-4 text-sm"><span className="font-serif text-lg font-semibold">{i+1}</span><span className="flex items-center gap-2 font-semibold text-ink"><GraduationCapIcon className="h-4 w-4 text-forest-700"/>{r.university}</span><span className="font-semibold text-forest-700">{r.problemsResolved}</span><span>{r.solutionsProvided}</span><span>{r.problemsWorkedOn}</span></div>)}</div>:<div className="mt-8 rounded-card border border-dashed border-line p-8 text-center text-sm text-ink-muted">University performance will appear here once teams provide verified solutions.</div>}</div></section>}
