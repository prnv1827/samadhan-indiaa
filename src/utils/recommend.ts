export function recommendPriority(title:string,description:string){const text=`${title} ${description}`.toLowerCase();const high=['unsafe','danger','emergency','accident','fire','flood','outbreak','collapsed','no drinking water','critical','death','injury'];const medium=['broken','blocked','overflow','outage','shortage','pollution','school','hospital'];const highHits=high.filter(k=>text.includes(k)).length;const medHits=medium.filter(k=>text.includes(k)).length;if(highHits>0)return {label:'High priority',reason:'The description contains safety or urgent-impact signals.',level:'high' as const};if(medHits>0)return {label:'Needs attention',reason:'The description contains service disruption or public-impact signals.',level:'medium' as const};return {label:'Standard priority',reason:'No urgent-impact signal was detected; the Admin can adjust priority during review.',level:'standard' as const};}
